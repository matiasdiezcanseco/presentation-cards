# Feature 02: Database Schema Design
**Status:** Completed

## Goal
Define the core data models for the application.

## Tasks
- [x] Define `users` table (if not fully covered by auth).
- [x] Define `cards` table (id, userId, title, width, height, background, etc.).
- [x] Define `elements` table (id, cardId, type, content, x, y, width, height, styles, zIndex).
- [x] Run `db:generate` and `db:push`.

